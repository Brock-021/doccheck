# DocCheck · 测试用例

**版本：** v0.1  
**对应需求：** DocCheck-需求文档.md v1.0  
**用例总数：** 98 条

---

## 一、用户认证（8条）

### TC-AUTH-001：正常登录
| 字段 | 值 |
|:----|:------|
| **前置条件** | 数据库中存在用户 admin / admin123 |
| **操作步骤** | 1. 访问 /login → 2. 输入用户名 admin，密码 admin123 → 3. 点击登录 |
| **预期结果** | 登录成功，页面跳转到 Dashboard |
| **优先级** | P0 |

### TC-AUTH-002：密码错误
| 字段 | 值 |
|:----|:------|
| **操作步骤** | 输入用户名 admin，密码 wrongpass，点击登录 |
| **预期结果** | 登录失败，提示"用户名或密码错误"，停留在登录页 |
| **优先级** | P0 |

### TC-AUTH-003：用户名不存在
| 字段 | 值 |
|:----|:------|
| **操作步骤** | 输入用户名 nonexist，密码任意，点击登录 |
| **预期结果** | 登录失败，提示"用户名或密码错误"，不暴露用户是否存在 |
| **优先级** | P1 |

### TC-AUTH-004：空用户名/密码
| 字段 | 值 |
|:----|:------|
| **操作步骤** | 用户名为空或密码为空，点击登录 |
| **预期结果** | 表单校验阻止提交，提示"请输入用户名"或"请输入密码" |
| **优先级** | P1 |

### TC-AUTH-005：登出
| 字段 | 值 |
|:----|:------|
| **前置条件** | 已登录 |
| **操作步骤** | 点击登出按钮 |
| **预期结果** | Session 清除，页面跳转到 /login |
| **优先级** | P0 |

### TC-AUTH-006：未登录访问受保护页面
| 字段 | 值 |
|:----|:------|
| **操作步骤** | 浏览器直接访问 /documents/upload |
| **预期结果** | 重定向到 /login，登录后回到原页面 |
| **优先级** | P0 |

### TC-AUTH-007：Session 过期
| 字段 | 值 |
|:----|:------|
| **前置条件** | 已登录，等待 session 过期（或强制清除 cookie）|
| **操作步骤** | 刷新页面或发起接口请求 |
| **预期结果** | 重定向到 /login |
| **优先级** | P1 |

### TC-AUTH-008：记住登录状态
| 字段 | 值 |
|:----|:------|
| **操作步骤** | 登录后关闭浏览器，重新打开访问页面 |
| **预期结果** | session 未过期时可访问，过期后需重新登录 |
| **优先级** | P2 |

---

## 二、文档类型管理（6条）

### TC-DOCTYPE-001：新增文档类型
| 字段 | 值 |
|:----|:------|
| **前置条件** | 以规则管理员身份登录 |
| **操作步骤** | 1. 进入文档类型管理 → 2. 点击新增 → 3. 输入名称"验收报告" → 4. 保存 |
| **预期结果** | 文档类型列表中新增"验收报告"，可正常显示 |
| **优先级** | P0 |

### TC-DOCTYPE-002：编辑文档类型
| 字段 | 值 |
|:----|:------|
| **操作步骤** | 编辑已有文档类型，将名称改为"验收测试报告" |
| **预期结果** | 名称更新成功 |
| **优先级** | P1 |

### TC-DOCTYPE-003：删除无关联的文档类型
| 字段 | 值 |
|:----|:------|
| **操作步骤** | 删除一个没有被任何规则引用的文档类型 |
| **预期结果** | 删除成功 |
| **优先级** | P0 |

### TC-DOCTYPE-004：删除有关联规则的文档类型
| 字段 | 值 |
|:----|:------|
| **前置条件** | 该文档类型下存在规则 |
| **操作步骤** | 点击删除 |
| **预期结果** | 提示"该文档类型下还有 N 条规则，请先处理规则再删除"，删除失败 |
| **优先级** | P0 |

### TC-DOCTYPE-005：文档类型排序
| 字段 | 值 |
|:----|:------|
| **操作步骤** | 调整文档类型的排序顺序 |
| **预期结果** | 上传页面的下拉列表中按排序号展示 |
| **优先级** | P2 |

### TC-DOCTYPE-006：文档类型名称重复
| 字段 | 值 |
|:----|:------|
| **操作步骤** | 新增一个已有的文档类型名称 |
| **预期结果** | 提示"文档类型名称已存在"，保存失败 |
| **优先级** | P1 |

---

## 三、规则管理（14条）

### TC-RULE-001：新增规则 — 完整字段
| 字段 | 值 |
|:----|:------|
| **前置条件** | 至少存在一个文档类型 |
| **操作步骤** | 填写规则名称、选择文档类型、填写规则描述、选择严重程度、排序号、启用 → 保存 |
| **预期结果** | 规则列表新增一条，所有字段显示正确 |
| **优先级** | P0 |

### TC-RULE-002：新增规则 — 必填字段缺失
| 字段 | 值 |
|:----|:------|
| **操作步骤** | 规则名称为空 或 规则描述为空 或 未选择文档类型 |
| **预期结果** | 表单校验阻止，提示对应必填项 |
| **优先级** | P0 |

### TC-RULE-003：编辑规则
| 字段 | 值 |
|:----|:------|
| **操作步骤** | 修改规则名称、描述、严重程度 → 保存 |
| **预期结果** | 更新成功，列表中显示新内容 |
| **优先级** | P0 |

### TC-RULE-004：删除未使用的规则
| 字段 | 值 |
|:----|:------|
| **操作步骤** | 删除一条没有被任何 check_task 引用的规则 |
| **预期结果** | 删除成功，规则列表不再显示 |
| **优先级** | P0 |

### TC-RULE-005：删除已被检查引用的规则
| 字段 | 值 |
|:----|:------|
| **前置条件** | 该规则已被至少一次检查任务引用 |
| **操作步骤** | 点击删除 |
| **预期结果** | 提示"该规则已被检查任务引用，将标记为已废弃"，标记后不可编辑但历史报告仍可显示 |
| **优先级** | P0 |

### TC-RULE-006：启用/禁用
| 字段 | 值 |
|:----|:------|
| **操作步骤** | 1. 禁用一条已启用的规则 → 2. 再启用 |
| **预期结果** | 状态切换正确，禁用的规则不参与检查 |
| **优先级** | P0 |

### TC-RULE-007：复制规则
| 字段 | 值 |
|:----|:------|
| **操作步骤** | 点击某规则的复制按钮 |
| **预期结果** | 生成一条新规则，内容与源规则一致，规则名称为"源名称(副本)" |
| **优先级** | P1 |

### TC-RULE-008：规则排序
| 字段 | 值 |
|:----|:------|
| **操作步骤** | 修改多条规则的排序号 |
| **预期结果** | 报告中按排序号升序展示 |
| **优先级** | P2 |

### TC-RULE-009：按文档类型筛选规则
| 字段 | 值 |
|:----|:------|
| **操作步骤** | 在规则列表中选择文档类型筛选 |
| **预期结果** | 只显示该文档类型下的规则 |
| **优先级** | P1 |

### TC-RULE-010：批量启用/禁用
| 字段 | 值 |
|:----|:------|
| **操作步骤** | 勾选多条规则，批量禁用 → 再批量启用 |
| **预期结果** | 全部选中规则的启用状态按操作切换 |
| **优先级** | P2 |

### TC-RULE-011：规则描述过长
| 字段 | 值 |
|:----|:------|
| **操作步骤** | 输入超长文本作为规则描述（如 5000 字）|
| **预期结果** | 保存成功，列表截断展示但详情页完整显示 |
| **优先级** | P3 |

### TC-RULE-012：规则分页显示
| 字段 | 值 |
|:----|:------|
| **前置条件** | 规则数量超过每页显示条数 |
| **操作步骤** | 翻页查看 |
| **预期结果** | 分页正常，数据不重复不遗漏 |
| **优先级** | P1 |

### TC-RULE-013：适用阶段 — 初检只匹配初审/通用
| 字段 | 值 |
|:----|:------|
| **前置条件** | 有初审、终审、通用三种阶段的规则 |
| **操作步骤** | 上传文档选择"初检" |
| **预期结果** | 检查时只匹配「初审 + 通用」阶段规则，不匹配「终审」规则 |
| **优先级** | P1 |

### TC-RULE-014：适用阶段 — 终检匹配终审/通用
| 字段 | 值 |
|:----|:------|
| **操作步骤** | 上传文档选择"终检" |
| **预期结果** | 检查时只匹配「终审 + 通用」阶段规则 |
| **优先级** | P1 |

---

## 四、文档上传（10条）

### TC-UPLOAD-001：正常上传 .docx
| 字段 | 值 |
|:----|:------|
| **前置条件** | 以编写者身份登录，已创建文档类型 |
| **操作步骤** | 选择文档类型 → 选择阶段 → 上传 .docx 文件 → 点击开始检查 |
| **预期结果** | 文件上传成功，页面显示"检查中"状态，check_task 状态为 running |
| **优先级** | P0 |

### TC-UPLOAD-002：上传非 .docx 文件
| 字段 | 值 |
|:----|:------|
| **操作步骤** | 上传 .pdf、.doc、.txt、.zip 文件 |
| **预期结果** | 提示"仅支持 .docx 格式"，文件不被接受 |
| **优先级** | P0 |

### TC-UPLOAD-003：上传超限文件
| 字段 | 值 |
|:----|:------|
| **操作步骤** | 上传大于 50MB 的 .docx 文件 |
| **预期结果** | 提示"文件大小不能超过 50MB"，上传失败 |
| **优先级** | P0 |

### TC-UPLOAD-004：上传加密文档
| 字段 | 值 |
|:----|:------|
| **操作步骤** | 上传带密码的 .docx 文档 |
| **预期结果** | python-docx 解析时报错，提示"无法解析加密文档" |
| **优先级** | P1 |

### TC-UPLOAD-005：上传空文档
| 字段 | 值 |
|:----|:------|
| **操作步骤** | 上传内容为空的 .docx 文件 |
| **预期结果** | 上传成功，检查结果中所有规则均标记为"无法判断"或提示文档内容为空 |
| **优先级** | P1 |

### TC-UPLOAD-006：不上传文件直接点开始检查
| 字段 | 值 |
|:----|:------|
| **操作步骤** | 选择文档类型后，点击开始检查 |
| **预期结果** | 提示"请先上传文件" |
| **优先级** | P1 |

### TC-UPLOAD-007：不选文档类型上传文件
| 字段 | 值 |
|:----|:------|
| **操作步骤** | 上传文件后不选文档类型，点击开始检查 |
| **预期结果** | 提示"请选择文档类型" |
| **优先级** | P1 |

### TC-UPLOAD-008：上传同名文件
| 字段 | 值 |
|:----|:------|
| **操作步骤** | 重复上传同名的 .docx 文件 |
| **预期结果** | 视为不同版本，各自保存，文件名自动加时间戳或序号 |
| **优先级** | P2 |

### TC-UPLOAD-009：上传中文文件名
| 字段 | 值 |
|:----|:------|
| **操作步骤** | 上传"XX项目立项报告_v2.1.docx" |
| **预期结果** | 上传成功，文件名在系统中显示正确（无乱码）|
| **优先级** | P1 |

### TC-UPLOAD-010：检查中页面反馈
| 字段 | 值 |
|:----|:------|
| **操作步骤** | 上传后等待检查完成 |
| **预期结果** | 页面显示进度状态（检查中→检查完成），或提供轮询接口供前端刷新 |
| **优先级** | P1 |

---

## 五、AI 检查引擎（8条）

### TC-CHECK-001：正常检查 — 文档符合所有规则
| 字段 | 值 |
|:----|:------|
| **前置条件** | 提交一份完全符合所有规则的文档 |
| **操作步骤** | 检查完成后查看报告 |
| **预期结果** | 所有规则结果 compliant=true，通过率 100% |
| **优先级** | P0 |

### TC-CHECK-002：正常检查 — 文档违反部分规则
| 字段 | 值 |
|:----|:------|
| **前置条件** | 提交一份有缺陷的文档（缺少章节、描述模糊）|
| **操作步骤** | 检查完成后查看报告 |
| **预期结果** | 对应规则 compliant=false，issue 和 suggestion 内容合理 |
| **优先级** | P0 |

### TC-CHECK-003：LLM 超时
| 字段 | 值 |
|:----|:------|
| **前置条件** | LLM 配置超时设为较短时间（如 3s）或 LLM 服务不可用 |
| **操作步骤** | 发起检查 |
| **预期结果** | check_task 状态变为 failed，提示"AI 检查超时，请稍后重试" |
| **优先级** | P0 |

### TC-CHECK-004：LLM 返回格式错误
| 字段 | 值 |
|:----|:------|
| **前置条件** | LLM 返回非 JSON 或 JSON 结构不匹配 |
| **操作步骤** | 发起检查，模拟 LLM 返回错误格式 |
| **预期结果** | 重试机制激活，重试后若仍失败则 check_task 状态变为 failed |
| **优先级** | P1 |

### TC-CHECK-005：LLM 返回空结果
| 字段 | 值 |
|:----|:------|
| **前置条件** | LLM 返回空数组或空内容 |
| **操作步骤** | 发起检查 |
| **预期结果** | 提示"未获取到检查结果，请重试" |
| **优先级** | P1 |

### TC-CHECK-006：规则数量为 0
| 字段 | 值 |
|:----|:------|
| **前置条件** | 该文档类型下没有已启用的规则 |
| **操作步骤** | 上传文档并检查 |
| **预期结果** | 提示"该文档类型下没有启用的检查规则，请先配置规则" |
| **优先级** | P0 |

### TC-CHECK-007：大文档检查（接近 50MB）
| 字段 | 值 |
|:----|:------|
| **前置条件** | 一份包含大量文本的 .docx（约 45MB）|
| **操作步骤** | 上传并检查 |
| **预期结果** | 检查完成，结果正常（可能耗时较长，但不超过 60s）|
| **优先级** | P1 |

### TC-CHECK-008：文档只有表格/图片无文本
| 字段 | 值 |
|:----|:------|
| **操作步骤** | 上传一份只有表格和图片、几乎无文字描述的 .docx |
| **预期结果** | python-docx 提取到有限文本，检查结果提示"文档文本内容较少，可能影响判断准确度" |
| **优先级** | P2 |

---

## 六、检查报告（8条）

### TC-REPORT-001：报告展示完整
| 字段 | 值 |
|:----|:------|
| **前置条件** | 一次检查已完成 |
| **操作步骤** | 访问报告页面 |
| **预期结果** | 显示文档名称、类型、检查时间、检查人、摘要统计卡片、逐条问题列表 |
| **优先级** | P0 |

### TC-REPORT-002：摘要统计正确
| 字段 | 值 |
|:----|:------|
| **操作步骤** | 核对摘要中的总规则数、通过数、问题数、通过率 |
| **预期结果** | 摘要与逐条列表一致，总规则数 = 通过数 + 问题数 |
| **优先级** | P0 |

### TC-REPORT-003：问题项颜色标记
| 字段 | 值 |
|:----|:------|
| **操作步骤** | 查看报告中的严重程度列 |
| **预期结果** | 🔴必须改 / 🟡建议改 / 🟢通过 / ⚪无法判断 — 颜色区分清晰 |
| **优先级** | P1 |

### TC-REPORT-004：重新检查
| 字段 | 值 |
|:----|:------|
| **操作步骤** | 点击"重新检查"，上传修改后的文档 |
| **预期结果** | 创建新的 check_task，原报告保留，形成版本历史 |
| **优先级** | P0 |

### TC-REPORT-005：报告页浏览器刷新
| 字段 | 值 |
|:----|:------|
| **操作步骤** | 查看报告后刷新页面 |
| **预期结果** | 报告内容正常显示，数据一致 |
| **优先级** | P2 |

### TC-REPORT-006：查看他人的报告
| 字段 | 值 |
|:----|:------|
| **前置条件** | 编写者A查看编写者B的报告 |
| **操作步骤** | 编写者A访问编写者B报告的 URL |
| **预期结果** | 审核人员和管理员可查看所有人报告；编写者只能查看自己的报告 |
| **优先级** | P1 |

### TC-REPORT-007：报告列表按时间排序
| 字段 | 值 |
|:----|:------|
| **操作步骤** | 查看文档历史列表 |
| **预期结果** | 按检查时间倒序排列，最新的在最上面 |
| **优先级** | P1 |

### TC-REPORT-008：报告导出按钮可见
| 字段 | 值 |
|:----|:------|
| **操作步骤** | 查看报告详情页 |
| **预期结果** | 「导出 PDF」和「导出 Word」按钮可见（v1.0 可先置灰提示"即将上线"）|
| **优先级** | P2 |

---

## 七、审核流程（10条）

### TC-REVIEW-001：确认问题
| 字段 | 值 |
|:----|:------|
| **前置条件** | 以审核人员身份登录，有一份待审核报告 |
| **操作步骤** | 点击某问题的 "确认" 按钮 |
| **预期结果** | 该问题状态变为"已确认"，显示操作人和操作时间 |
| **优先级** | P0 |

### TC-REVIEW-002：驳回问题
| 字段 | 值 |
|:----|:------|
| **操作步骤** | 点击 "驳回" 按钮，输入驳回理由 |
| **预期结果** | 问题状态变为"已驳回"，显示驳回理由、操作人和操作时间 |
| **优先级** | P0 |

### TC-REVIEW-003：忽略问题
| 字段 | 值 |
|:----|:------|
| **操作步骤** | 点击 "忽略" 按钮，输入忽略备注 |
| **预期结果** | 问题状态变为"已忽略"，显示备注内容 |
| **优先级** | P0 |

### TC-REVIEW-004：审核结论 — 通过
| 字段 | 值 |
|:----|:------|
| **前置条件** | 所有问题已审核，无未确认的「必须改」问题 |
| **操作步骤** | 点击"出结论" → 选择"通过" |
| **预期结果** | 审核结论为「✅ 通过」，报告状态更新 |
| **优先级** | P0 |

### TC-REVIEW-005：审核结论 — 有条件通过
| 字段 | 值 |
|:----|:------|
| **前置条件** | 存在「必须改」问题但已被确认 |
| **操作步骤** | 点击"出结论" → 选择"有条件通过" |
| **预期结果** | 审核结论为「⚠️ 有条件通过」，建议修改后重新检查 |
| **优先级** | P0 |

### TC-REVIEW-006：审核结论 — 不通过
| 字段 | 值 |
|:----|:------|
| **前置条件** | 存在「必须改」问题且尚未确认 |
| **操作步骤** | 点击"出结论" → 提示"还有未确认的必须改问题，请先处理" |
| **预期结果** | 不能选择"不通过"之需先确认—实际应为必须先处理必须改问题 |
| **优先级** | P1 |

### TC-REVIEW-007：审核中的问题不能重复操作
| 字段 | 值 |
|:----|:------|
| **操作步骤** | 对已确认的问题再次点击确认 |
| **预期结果** | 按钮置灰或提示"该问题已审核" |
| **优先级** | P1 |

### TC-REVIEW-008：审核操作计入审计日志
| 字段 | 值 |
|:----|:------|
| **操作步骤** | 执行确认/驳回/忽略操作后，查看审计日志 |
| **预期结果** | 审计日志中记录操作人、操作时间、操作类型（confirm/reject/ignore）、报告 ID |
| **优先级** | P1 |

### TC-REVIEW-009：编写者不能审核
| 字段 | 值 |
|:----|:------|
| **前置条件** | 以编写者身份登录 |
| **操作步骤** | 访问 /reviews/{report_id} |
| **预期结果** | 显示"无权限"或重定向到首页，审核按钮不可见 |
| **优先级** | P0 |

### TC-REVIEW-010：多人审核同一报告
| 字段 | 值 |
|:----|:------|
| **前置条件** | 审核人员A和审核人员B |
| **操作步骤** | A确认了问题1，B刷新页面 |
| **预期结果** | B看到问题1已被确认，不能重复操作 |
| **优先级** | P2 |

---

## 八、历史记录（4条）

### TC-HIST-001：文档历史列表
| 字段 | 值 |
|:----|:------|
| **前置条件** | 同一文档至少被检查过2次 |
| **操作步骤** | 进入文档历史页 |
| **预期结果** | 按时间倒序列出历次检查记录，显示检查时间、规则数、问题数、审核状态 |
| **优先级** | P1 |

### TC-HIST-002：查看某次历史报告
| 字段 | 值 |
|:----|:------|
| **操作步骤** | 点击某次历史记录 |
| **预期结果** | 跳转到该次检查的报告详情页 |
| **优先级** | P1 |

### TC-HIST-003：版本对比
| 字段 | 值 |
|:----|:------|
| **前置条件** | 同一文档至少被检查过2次 |
| **操作步骤** | 选择两个版本进行对比 |
| **预期结果** | 并排显示差异，标记新增问题、已解决问题、仍然存在的问题 |
| **优先级** | P2 |

### TC-HIST-004：查看不属于自己的文档历史
| 字段 | 值 |
|:----|:------|
| **操作步骤** | 编写者查看他人的文档 |
| **预期结果** | 看不到他人的文档列表（审核人员/管理员可查看全部）|
| **优先级** | P1 |

---

## 九、报告导出（4条）

### TC-EXPORT-001：导出 PDF
| 字段 | 值 |
|:----|:------|
| **前置条件** | 一份完整的检查报告 |
| **操作步骤** | 点击"导出 PDF" |
| **预期结果** | 下载一个 PDF 文件，包含报告完整内容，中文显示正常 |
| **优先级** | P1 |

### TC-EXPORT-002：导出 Word
| 字段 | 值 |
|:----|:------|
| **操作步骤** | 点击"导出 Word" |
| **预期结果** | 下载一个 .docx 文件，包含报告完整内容，格式排版整洁 |
| **优先级** | P1 |

### TC-EXPORT-003：导出报告中文不乱码
| 字段 | 值 |
|:----|:------|
| **操作步骤** | 导出包含中文内容的报告 |
| **预期结果** | PDF/Word 中所有中文字符显示正常，无乱码、无方框 |
| **优先级** | P1 |

### TC-EXPORT-004：没有权限的用户导出
| 字段 | 值 |
|:----|:------|
| **操作步骤** | 编写者看不到不属于自己的报告的导出按钮 |
| **预期结果** | 同理权限控制 |
| **优先级** | P2 |

---

## 十、LLM 配置（6条）

### TC-LLM-001：保存 LLM 配置
| 字段 | 值 |
|:----|:------|
| **前置条件** | 以系统管理员身份登录 |
| **操作步骤** | 填写 API 地址、Key、模型名称 → 保存 |
| **预期结果** | 配置保存成功，刷新页面对应字段仍显示 |
| **优先级** | P1 |

### TC-LLM-002：读取 LLM 配置
| 字段 | 值 |
|:----|:------|
| **操作步骤** | 打开 LLM 配置页面 |
| **预期结果** | 已保存的配置正确回显（Key 部分脱敏显示，如 "sk-****abcd"）|
| **优先级** | P1 |

### TC-LLM-003：测试连接 — 成功
| 字段 | 值 |
|:----|:------|
| **前置条件** | LLM 服务正常 |
| **操作步骤** | 点击"测试连接" |
| **预期结果** | 提示"连接成功"，显示可用模型列表 |
| **优先级** | P1 |

### TC-LLM-004：测试连接 — 失败
| 字段 | 值 |
|:----|:------|
| **前置条件** | LLM 地址错误或服务不可用 |
| **操作步骤** | 点击"测试连接" |
| **预期结果** | 提示"连接失败：原因"，不阻塞配置保存 |
| **优先级** | P1 |

### TC-LLM-005：API Key 不为空校验
| 字段 | 值 |
|:----|:------|
| **操作步骤** | 将 API Key 清空后保存 |
| **预期结果** | 提示"API Key 不能为空" |
| **优先级** | P1 |

### TC-LLM-006：非管理员访问 LLM 配置
| 字段 | 值 |
|:----|:------|
| **操作步骤** | 编写者/审核员直接访问 /admin/llm-config |
| **预期结果** | 提示"无权限"或重定向 |
| **优先级** | P0 |

---

## 十一、用户管理（8条）

### TC-USER-001：用户列表展示
| 字段 | 值 |
|:----|:------|
| **前置条件** | 以系统管理员身份登录 |
| **操作步骤** | 进入用户管理页面 |
| **预期结果** | 显示所有用户，包含用户名、姓名、角色、状态、最后登录时间 |
| **优先级** | P1 |

### TC-USER-002：新增用户
| 字段 | 值 |
|:----|:------|
| **操作步骤** | 填写用户名、姓名、密码、选择角色 → 保存 |
| **预期结果** | 用户新增成功，出现在列表中，可用新账号登录 |
| **优先级** | P1 |

### TC-USER-003：编辑用户
| 字段 | 值 |
|:----|:------|
| **操作步骤** | 修改用户姓名、角色 → 保存 |
| **预期结果** | 更新成功 |
| **优先级** | P1 |

### TC-USER-004：禁用用户
| 字段 | 值 |
|:----|:------|
| **操作步骤** | 禁用某一用户 |
| **预期结果** | 该用户账户状态变"禁用"，该用户无法登录 |
| **优先级** | P1 |

### TC-USER-005：启用用户
| 字段 | 值 |
|:----|:------|
| **操作步骤** | 启用被禁用的用户 |
| **预期结果** | 用户可正常登录 |
| **优先级** | P1 |

### TC-USER-006：重置密码
| 字段 | 值 |
|:----|:------|
| **操作步骤** | 点击"重置密码"，输入新密码 |
| **预期结果** | 密码更新成功，用户可用新密码登录 |
| **优先级** | P1 |

### TC-USER-007：用户名重复
| 字段 | 值 |
|:----|:------|
| **操作步骤** | 新增一个已存在的用户名 |
| **预期结果** | 提示"用户名已存在" |
| **优先级** | P1 |

### TC-USER-008：删除自己的账号
| 字段 | 值 |
|:----|:------|
| **操作步骤** | 当前管理员试图删除自己的账号 |
| **预期结果** | 提示"不能删除当前登录账号"或"删除后会话将失效" |
| **优先级** | P2 |

---

## 十二、审计日志（4条）

### TC-AUDIT-001：操作记录正确
| 字段 | 值 |
|:----|:------|
| **前置条件** | 执行过规则新增、文档检查、审核操作 |
| **操作步骤** | 进入审计日志页面 |
| **预期结果** | 所有操作均有记录，包含操作人、时间、类型、IP、详情 |
| **优先级** | P2 |

### TC-AUDIT-002：按时间筛选
| 字段 | 值 |
|:----|:------|
| **操作步骤** | 选择开始时间和结束时间筛选 |
| **预期结果** | 只显示该时间范围内的操作记录 |
| **优先级** | P2 |

### TC-AUDIT-003：按操作类型筛选
| 字段 | 值 |
|:----|:------|
| **操作步骤** | 选择操作类型（如"审核操作"）筛选 |
| **预期结果** | 只显示该类型的操作记录 |
| **优先级** | P2 |

### TC-AUDIT-004：审计日志分页
| 字段 | 值 |
|:----|:------|
| **前置条件** | 审计日志超过每页条数 |
| **操作步骤** | 翻页查看 |
| **预期结果** | 分页正常，数据不重复不遗漏 |
| **优先级** | P2 |

---

## 十三、权限控制（10条）

### TC-PERM-001：编写者不能访问规则管理
| 字段 | 值 |
|:----|:------|
| **操作步骤** | 编写者直接访问 /admin/rules |
| **预期结果** | 提示"无权限"或重定向 |
| **优先级** | P0 |

### TC-PERM-002：编写者不能访问用户管理
| 字段 | 值 |
|:----|:------|
| **操作步骤** | 编写者访问 /admin/users |
| **预期结果** | 提示"无权限" |
| **优先级** | P0 |

### TC-PERM-003：编写者不能访问 LLM 配置
| 字段 | 值 |
|:----|:------|
| **操作步骤** | 编写者访问 /admin/llm-config |
| **预期结果** | 提示"无权限" |
| **优先级** | P0 |

### TC-PERM-004：审核人员无法上传文档
| 字段 | 值 |
|:----|:------|
| **前置条件** | 用户仅有审核角色 |
| **操作步骤** | 访问 /documents/upload |
| **预期结果** | 提示"无权限"或按钮隐藏 |
| **优先级** | P1 |

### TC-PERM-005：规则管理员不能审核
| 字段 | 值 |
|:----|:------|
| **前置条件** | 用户仅有规则管理员角色 |
| **操作步骤** | 访问 /reviews/{report_id} |
| **预期结果** | 提示"无权限" |
| **优先级** | P1 |

### TC-PERM-006：管理员拥有所有权限
| 字段 | 值 |
|:----|:------|
| **操作步骤** | 管理员访问所有页面 |
| **预期结果** | 所有页面均可访问，所有操作均可执行 |
| **优先级** | P0 |

### TC-PERM-007：接口直接调用越权
| 字段 | 值 |
|:----|:------|
| **操作步骤** | 编写者直接 POST /api/admin/rules （新增规则）|
| **预期结果** | 返回 403 Forbidden |
| **优先级** | P0 |

### TC-PERM-008：多角色用户功能全面
| 字段 | 值 |
|:----|:------|
| **前置条件** | 用户同时拥有编写者+审核角色 |
| **操作步骤** | 验证能上传文档，也能审核报告 |
| **预期结果** | 两个角色的功能均可用 |
| **优先级** | P1 |

### TC-PERM-009：API Key 不在前端暴露
| 字段 | 值 |
|:----|:------|
| **操作步骤** | 查看 LLM 配置页面源代码 |
| **预期结果** | API Key 不会以明文出现在 HTML 源码或 JS 中 |
| **优先级** | P1 |

### TC-PERM-010：禁用用户不能登录
| 字段 | 值 |
|:----|:------|
| **前置条件** | 某用户被禁用 |
| **操作步骤** | 尝试用该用户账号登录 |
| **预期结果** | 登录失败，提示"账户已被禁用，请联系管理员" |
| **优先级** | P0 |

---

> **用例统计：** 共 98 条  
> **P0：** 30 条 | **P1：** 43 条 | **P2：** 21 条 | **P3：** 4 条
